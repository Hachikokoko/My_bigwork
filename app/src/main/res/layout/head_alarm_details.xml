<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/head_height"
    android:background="@color/colorBlue">

    <FrameLayout
        android:id="@+id/alarm_cancel"
        android:layout_width="70dp"
        android:layout_height="@dimen/head_height"
        android:layout_alignParentLeft="true"
        android:background="@drawable/alarm_head_bg_pressed"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/back"
            android:layout_width="30dp"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
            android:contentDescription="@string/head_image_des"
            android:src="@drawable/back_white" />

    </FrameLayout>

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="添加事务"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="@color/white" />

    <RelativeLayout
        android:id="@+id/alarm_save"
        android:layout_width="@dimen/head_height"
        android:layout_height="@dimen/head_height"
        android:layout_alignParentRight="true"
        android:background="@drawable/alarm_head_bg_pressed">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_centerInParent="true"
            android:contentDescription="@string/head_image_des"
            android:src="@drawable/baocun" />
    </RelativeLayout>

</RelativeLayout>
